
let val=20.94
let x=40
let y=10.73
let z=-10

//console.log(Math.round(y)) // round of any value
console.log(`round of value of y=${y} is ${Math.round(y)}`) 

// if there is anything above absolute it will consider it next number
console.log(`ceil of value of val=${val} is ${Math.ceil(val)}`) 

// if there is anything above absolute it will take it down
console.log(`floor of value of val=${val} is ${Math.floor(val)}`) 

//console.log(Math.abs(z)) // provide absolute value meansing convert -ve -> +ve only
console.log(`absolute of value of z=${z} is ${Math.abs(z)}`)


const min=100
const max=200
console.log(Math.floor(Math.random()*(max-min+1)+min))


